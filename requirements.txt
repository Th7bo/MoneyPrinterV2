wheel
termcolor
schedule
requests
kittentts @ https://github.com/KittenML/KittenTTS/releases/download/0.8.1/kittentts-0.8.1-py3-none-any.whl
soundfile
prettytable
webdriver_manager
selenium_firefox
# selenium_firefox imports FirefoxBinary from Selenium's legacy module path.
# Selenium 4.17 deprecated that class and newer releases eventually removed it.
selenium==4.16.0
ollama
# The video pipeline still uses the MoviePy 1.x API (moviepy.editor,
# set_*/resize/fx helpers, config.change_settings and SubtitlesClip).
moviepy==1.0.3
# MoviePy 1.0.3 uses Pillow APIs removed in Pillow 10.
Pillow>=9.2,<10

yagmail
assemblyai
faster-whisper
srt_equalizer
undetected_chromedriver
platformdirs
